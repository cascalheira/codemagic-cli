name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Both macOS jobs run on Apple Silicon; x86-64 is cross-compiled.
          - name: macOS · x86-64 (cross-compiled)
            runner: macos-latest
            target: x86_64-apple-darwin

          - name: macOS · arm64
            runner: macos-latest
            target: aarch64-apple-darwin

          - name: Linux · x86-64
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu

          - name: Linux · arm64
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu

          # ── Windows ─────────────────────────────────────────────────────────
          # Both Windows jobs run on the x86-64 hosted runner.
          # ARM64 is cross-compiled via MSVC's built-in ARM64 toolchain.
          - name: Windows · x86-64
            runner: windows-latest
            target: x86_64-pc-windows-msvc

          - name: Windows · arm64 (cross-compiled)
            runner: windows-latest
            target: aarch64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v5

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
          components: clippy

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ci-${{ matrix.target }}

      # The GUI app (`gantry`) defaults to the `desktop` feature, which on Linux
      # links against the GTK/WebKitGTK stack. Those aren't preinstalled on the
      # runners, so they're installed here — otherwise the app would only ever
      # be compiled on macOS and Windows, and we ship a Linux build now.
      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            pkg-config libssl-dev \
            libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev libxdo-dev

      - name: Build (debug)
        run: cargo build --locked --target ${{ matrix.target }}

      - name: Clippy
        run: cargo clippy --locked --target ${{ matrix.target }} -- -D warnings
